const initialState = {
  pending: false,
  counters: [],
  error: null,
  createPending: false,
  createError: null,
  togglePending: false,
  toggleError: false,
  toggleId: '',
  selectedCounterId: '',
  pendingToggle: false,
}

export default initialState
